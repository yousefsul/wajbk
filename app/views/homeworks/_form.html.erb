<%= form_with(model: homework, local: true) do |form| %>
  <% if homework.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(homework.errors.count, "error") %> prohibited this homework from being saved:</h2>

      <ul>
      <% homework.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :speciality %>
    <%= form.text_field :speciality, id: :homework_speciality %>
  </div>

  <div class="field">
    <%= form.label :course_name %>
    <%= form.text_field :course_name, id: :homework_course_name %>
  </div>

  <div class="field">
    <%= form.label :question %>
    <%= form.text_area :question, id: :homework_question %>
  </div>

  <div class="field">
    <%= form.label :submission_deadline %>
    <%= form.date_select :submission_deadline, id: :homework_submission_deadline %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
